apiVersion: apps/v1
kind: Deployment
metadata:
  name: users
  namespace: staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users
  template:
    metadata:
      labels:
        app: users
    spec:
      containers:
        - name: users
          image: arlg92/users-service:latest
          ports:
            - containerPort: 8000
